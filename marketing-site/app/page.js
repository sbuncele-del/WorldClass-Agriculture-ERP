import Link from 'next/link';
import Image from 'next/image';
import {pages} from './content';
export default function Home(){const p=pages['agriculture-erp-platform'];return <main><section className="pageHero home"><Image src={p.image} alt="Commercial agricultural enterprise" fill priority sizes="100vw"/><div className="shade"/><div className="heroCopy"><span className="eyebrow">THE FARM, UNDER COMMAND</span><h1>Run the whole farm from one operating picture.</h1><p>Field work, irrigation, crops, livestock, inputs, harvest and financial control—connected to live signals and built on a proven ERP engine.</p><div className="actions"><Link className="primary" href="/agriculture-erp-platform">Explore the platform →</Link><a className="secondary" href="#solutions">View solutions</a></div></div></section><section id="solutions" className="cards"><span className="eyebrow">ONE FARM. ONE SYSTEM.</span><h2>Control every layer of the agricultural enterprise.</h2><div className="cardgrid">{Object.entries(pages).slice(1).map(([slug,x])=><Link href={`/${slug}`} className="card" key={slug}><Image src={x.image} alt="" fill sizes="(max-width: 800px) 100vw, 50vw"/><span className="cardshade"/><div><small>{x.kicker}</small><h3>{x.title}</h3><b>Explore capability →</b></div></Link>)}</div></section></main>}
