# Copy to .env on the isolated pilot host and replace all values.
# Never commit the real .env file.
FARMOS_DB_NAME=farmos_pilot
FARMOS_DB_USER=farmos_pilot
FARMOS_DB_PASSWORD=replace-with-a-long-random-secret
FARMOS_HTTP_PORT=8080
